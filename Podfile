# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'FMDB_Demo' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for FMDB_Demo
  
  pod 'FMDB'
  pod 'FMDB/FTS'
  pod 'FMDB/standalone'
  pod 'FMDB/standalone/FTS'
  pod 'FMDB/SQLCipher'
  

  target 'FMDB_DemoTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'FMDB_DemoUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
